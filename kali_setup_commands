
apt-get install [package]
# to list debian packages installed
dpkg -l 
dpkg -l | grep zip
# full info on package
apt-cache show zip
# To remove a package
apt-get remove [packagename]

# history commands - package installation 
apt-get update
apt-get upgrade -y
apt-get dist-upgrade -y
leafpad kali_update.sh
chmod +x kali_update.sh
./kali_update.sh 
dpkg -l
dpkg -l | grep zip
apt-cache show zip
vim /etc/apt/sources.list
apt-get install preload
apt-get install bleachbit
apt-get  install bum
apt-get gnome-do
apt-get install gnome-do
apt-get install apt-file
apt-get install scrub
apt-get install shutter 
apt-get install figlet
vim /root/.bashrc

cd /etc/ssh
ls
mkdir key_backup_ssh
mv ssh_host_*  backup_ssh
mv ssh_host_*  key_backup_ssh/
ls
dpkg-reconfigure openssh-server
service ssh restart
netstat -antp

history 50 | cut -c 8- >>kali_commands 
# to stop ssh
service ssh stop

mkdir key_backup_ssh
mv ssh_host_*  key_backup_ssh/
dpkg-reconfigure openssh-server
service ssh restart
netstat -antp
apt-get install tor
vim /etc/proxychains.conf
service tor start
service tor status
iceweasel www.whatismyip.com
proxychain iceweasel www.whatismyip.com
proxychains iceweasel www.whatismyip.com
chkrootkit

metagoofil -d tomgdow.com -t doc
metagoofil -d tomgdow.com  -t doc -o temp -f tomgdow.html
apt-get install metagoofil
metagoofil -d tomgdow.com  -t doc -o temp -f tomgdow.html
theharvester -help
theharvester -d tomgdow.com -b google -l 100
whois tomgdow.com
fierce -dns tomgdow.com
dmitry -winsepfbo tomgdow.com
git clone https://github.com/leebaird/discover.git
