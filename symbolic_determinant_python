PYTHON CODE

    >>> import sympy as sp
    >>> a, b, c, d = sp.symbols('a,b,c,d')
    >>> arr3 = sp.Matrix(([a,b],[c,d]))
    >>> print(sp.det(arr3))
    a*d - b*c


REFERENCE
   StackOverflow
   https://stackoverflow.com/a/68591662/499167
